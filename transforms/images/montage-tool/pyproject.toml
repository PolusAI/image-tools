[tool.poetry]
name = "polus-images-transforms-images-montage"
version = "0.5.2-dev0"
description = ""
authors = ["Nicholas Schaub <nick.schaub@nih.gov>"]
readme = "README.md"
packages = [{include = "polus", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.13"
bfio = ">=2.5.0"
filepattern = ">=2.2.1"
typer = ">=0.24.0"

[tool.poetry.group.dev.dependencies]
bump2version = "^1.0.1"
pre-commit = "^3.0.4"
flake8 = "^6.0.0"
black = "^23.1.0"
mypy = "^1.0.0"
pytest = "^7.2.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
